# Деплой и инфраструктура

:::{note}
Черновик — заполняется в сессии **J7** (см. `JUPYTER_BOOK_DOCS_PLAN.md`).
:::

Будет: сервисы Railway, Docker Compose локально, переменные окружения,
известные грабли (сегфолт `pyarrow`, torch/torchvision в `pipelines`).
