# База данных и хранилище

:::{note}
Черновик — заполняется в сессии **J4** (см. `JUPYTER_BOOK_DOCS_PLAN.md`).
:::

Будет: полная схема `db.py` (все таблицы), `storage.py` (Railway Bucket),
краткая историческая справка (Snowflake → SQLite → Postgres).
