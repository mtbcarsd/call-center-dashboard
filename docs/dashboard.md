# Dash-дашборд

:::{note}
Черновик — заполняется в сессии **J6** (см. `JUPYTER_BOOK_DOCS_PLAN.md`).
:::

Будет: страницы `dash_app/pages/`, компоненты `dash_app/components/`, паттерны
разработки (`layout()`+`@callback`, `dcc.Store`, pattern-matching id, тема
светлая/тёмная).
