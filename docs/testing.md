# Тестирование

:::{note}
Черновик — заполняется в сессии **J8** (см. `JUPYTER_BOOK_DOCS_PLAN.md`).
:::

Будет: структура `tests/`, подход к тестированию Dash-callback'ов,
WER-тестирование (`scripts/compute_wer.py`).
