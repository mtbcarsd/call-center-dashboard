# Скрипты и вспомогательные утилиты

:::{note}
Черновик — заполняется в сессии **J5** (см. `JUPYTER_BOOK_DOCS_PLAN.md`).
:::

Будет: `scripts/*.py` — когда каким пользоваться, паттерн «новые звонки —
INSERT-only скрипт, не `pipeline.py`».
