# REST API

:::{note}
Черновик — заполняется в сессии **J5** (см. `JUPYTER_BOOK_DOCS_PLAN.md`).
:::

Будет: эндпоинты `api/main.py` (`POST /analyze`, `GET /trends`,
`GET /coaching/{operator_name}`, `WS /transcribe/stream`) с примерами
запрос/ответ.
