# Хендофф для следующей сессии

Обновляется в конце каждой сессии. Если этот файл разошёлся с реальностью
(например, пункт уже сделан, а тут не отмечен) — верить коду и `git log`, а не
этому файлу, и поправить его заодно.

## Текущий статус (коротко)

Полная картина — в README.md → «Статус деплоя» и «Куда развиваться». Тут только то,
что нужно держать в голове перед тем как начинать новую задачу:

- **Локально (`docker-compose up`)** — весь стек работает без ограничений: API,
  OpenWebUI-чат, batch-pipeline, Grafana, Postgres.
- **Railway** — `call-center-dashboard` (Streamlit), `openwebui`, `Postgres`,
  `call-audio` (bucket) — работают. `api` и `pipelines` — **CRASHED/FAILED**, упираются
  в 1GB RAM Trial-плана (torch+whisper+pyannote не влезают). Апгрейд на Hobby отложен
  пользователем на неопределённый срок — не предлагать сделать это самостоятельно,
  просто иметь в виду при вопросах про доступность чата на проде.
- **Streamlit-дашборд** — Phase 1 и Phase 2 сделаны и задеплоены (см. ниже). Есть
  особенность: **любая правка requirements.txt без явного пина версии рискует
  сегфолтом** — на этом стеке уже трижды ловили сегфолт из-за неопределённых версий
  pandas/pyarrow/psycopg2 (см. git log, коммиты про "Fix segfault"/"Pin pyarrow").
  Перед добавлением новой зависимости в requirements.txt — пинить версию явно.
- **Данные**: 11 звонков (5 ОО + 5 ОРККиП + 1 CARtest), синхронизированы в локальной
  и Railway Postgres, аудио для всех залито в bucket.
- **Roadmap**: README.md → «Куда развиваться» — Phase 1-2 done, Phase 3 next
  (не расписана по шагам, см. ниже), Phase 4-5 расписаны, но не в приоритете.

## Фаза 2 — организация данных — ✅ ПОЛНОСТЬЮ ЗАВЕРШЕНА (сессия 8, 2026-07-18)

Все четыре пункта сделаны, протестированы (`AppTest` + прямая проверка Postgres) и
задеплоены на Railway (`SUCCESS`, прод отвечает 200, логи чистые):

- **2.1 Ручная валидация AI-категорий** — `call_type_override`, кнопки
  ✅ Подтвердить / ❌ Исправить в деталке звонка.
- **2.2 Дикторы** — `operator_name`, редактирование в деталке, вкладка «Операторы»
  со статистикой.
- **2.3 Теги и коллекции** — таблицы `tags`/`call_tags`/`collections`/
  `call_collections`, редактор в деталке, фильтры в сайдбаре.
- **2.4 Комментарии** — таблица `comments`, список + форма под транскриптом.

Детали каждого пункта (что именно сделано, какие решения приняты) — в
`memory_session.md` (память проекта), сессия 8, если понадобятся подробности сверх
этого файла.

**Важный урок, пригодится для будущих виджетов с БД-состоянием:** если
`st.multiselect`/`st.text_area`/другой виджет с `key` одновременно управляется и
пользовательским вводом, и внешним состоянием (БД), то `default`/`value` не
подхватится на повторных рендерах, если `key` не меняется — Streamlit держит старое
значение виджета из session_state. Фикс — включать хэш/состав актуальных данных в
сам `key` (см. `render_label_editor` и `add_comment`-текстовое поле в `dashboard.py`).

## Что дальше — Phase 3

README.md → «Куда развиваться» → Phase 3 перечисляет 4 идеи («то, что уже посчитано,
но не показано» — compliance-тренды, вкладка Тренды из `agents/trends.py`, рейтинг
операторов по чек-листу, ручная QA-оценка отдельно от AI). Это **список идей, не
готовый план** — в отличие от Фазы 2, тут ещё не расписано по шагам (схема/UI/тесты
для каждого пункта). Следующей сессии нужно сначала прочитать эти 4 пункта в README,
проверить, что упомянутый код (`agents/trends.py`, `checklist.py`, `GET /trends`)
всё ещё существует и работает так, как описано, и только потом составить конкретный
план по образцу Фазы 2 (по возрастанию сложности, с явными решениями по схеме БД).

## Промт для быстрого старта следующей сессии

Скопировать и вставить как первое сообщение:

```
Продолжаем call_center_dashboard. Прочитай NEXT_SESSION.md в корне репозитория —
Фаза 2 полностью завершена и задеплоена. Дальше — Phase 3 (README.md → «Куда
развиваться»), но она пока не расписана по шагам, в отличие от Фазы 2. Прочитай
4 пункта Phase 3 в README, проверь, что упомянутый код (agents/trends.py,
checklist.py, GET /trends) актуален, и предложи мне конкретный план по образцу
Фазы 2 — я подтвержу, с чего начать, прежде чем ты начнёшь писать код.
```

Если контекст нужен не по Фазе 3, а что-то другое — всё равно сначала прочитать
README.md → «Статус деплоя» и «Куда развиваться», чтобы не предлагать то, что уже
сделано или уже отклонено (см. пункт про Railway Hobby-план выше).
