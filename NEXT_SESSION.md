# Хендофф для следующей сессии

Обновляется в конце каждой сессии. Если этот файл разошёлся с реальностью
(например, пункт уже сделан, а тут не отмечен) — верить коду и `git log`, а не
этому файлу, и поправить его заодно.

## Текущий статус (коротко)

Полная картина — в README.md → «Статус деплоя» и «Куда развиваться». Тут только то,
что нужно держать в голове перед тем как начинать новую задачу:

- **✅ Миграция на Dash (D1-D5) — ПОЛНОСТЬЮ ЗАВЕРШЕНА (2026-07-22 — 2026-07-24).**
  `dash-director` — теперь **основной публичный дашборд**:
  **https://dash-director-production.up.railway.app**. Полный паритет со
  Streamlit-версией + больше: ролевой доступ (executive/manager/employee),
  личный кабинет сотрудника с LLM-коучингом, вкладка «Звонки» с плеером/
  инлайн-редакторами/тегами/комментариями. 118 юнит-тестов, 1 skip. Детальный
  план и статус по фазам — [DASH_MIGRATION_PLAN.md](DASH_MIGRATION_PLAN.md).
  - Учётки: `julia` (executive, все отделы), `boss_oo`/`boss_orkki` (manager,
    свой отдел), `sokolova` (employee, личный кабинет) — пароли в
    `scripts/seed_users.py`.
  - **Мягкий cutover, не полное отключение**: Streamlit
    (`call-center-dashboard-production-6a6b.up.railway.app`) оставлен жить как
    страховка на откат, срок отключения не назначен — пользователь явно
    выбрал этот вариант вместо изначально согласованного «полностью
    переключиться и выключить» (см. DASH_MIGRATION_PLAN.md → «Подтверждённые
    решения»).
  - Кастомного домена в проекте нет — оба сервиса на `*.up.railway.app`.
- **Синтетика для дневных/часовых графиков (2026-07-22, эта же сессия)**:
  добавлено 320 облегчённых синтетических звонков (`scripts/
  generate_synthetic_metrics_calls.py`) — без аудио/LLM, только правдоподобный
  разброс `call_datetime` по ~90 дням (будни > выходных, пик 9-11/15-16) и
  метрики по образцу распределений в реальных данных. И в локальной, и в
  Railway Postgres. **Не использовано** — страницы «по дням»/«по часам» в
  духе польского Power BI-референса не строили, данные просто лежат и ждут.
- **Railway — план апгрейднут на Hobby (2026-07-18)**. RAM не блокер: `api`
  работает чисто. `GET /trends` и `GET /coaching/{operator_name}` проверены
  end-to-end (реально ходят в Groq). LLM-бэкенд — Groq, облачный.
- **`pipelines` (OpenWebUI-чат) — всё ещё НЕ РАБОТАЕТ**, по-прежнему не из-за
  RAM: `ghcr.io/open-webui/pipelines:main` несёт несовместимый
  torch/torchvision/torchaudio. Подробности фикса и необъяснённого расхождения
  build/runtime — см. предыдущие версии этого файла в git log (сессии 6-8) или
  `memory_session.md`. **Сознательно отложено**, не в приоритете.
- **Данные**: 341 звонок на Railway (340 локально — старое расхождение на 1
  запись, `CARtest`, не трогали). 20 «настоящих» (10 оригинальных из аудио-
  сэмплов без `call_datetime`, 10 именованных синтетических с сессии 9) + 320
  облегчённых синтетических с сессии 24-го (только метрики, `call_summary`
  вида «Синтетический звонок (ОО): …», `transcript_text IS NULL`).
- **Streamlit-дашборд** — легаси, дальше не развивается. Осторожность на
  будущее (если вдруг вернёмся туда): любая правка `requirements.txt` без
  явного пина версии рискует сегфолтом (см. git log, "Fix segfault"/"Pin
  pyarrow").

## Что дальше

Явного продолжения плана (как D1→D5) больше нет — миграция закрыта. Варианты
на выбор, ничего не согласовано заранее:

**A. Дневной/часовой разрез на Dash-дашборде** (дёшево — данные уже готовы,
см. синтетику выше) — страница/график в духе `WYNIKI DZIENNE`/`WYNIKI
GODZINOWE` из польского Power BI-референса, который смотрели в начале этой
сессии. Данных для содержательного графика теперь достаточно (320+20 звонков
за ~3 месяца с реальным разбросом по будням/часам).

**B. Phase 4/5 из README.md → «Куда развиваться»** — списки идей, не готовый
план:
1. Встроенный AI-чат (Phase 4) зависит от `pipelines`, который не работает —
   см. выше.
2. Live-режим (`WS /transcribe/stream` в `api/main.py` уже есть, не
   используется в UI) и экспорт отчётов (PDF/CSV) от `pipelines` НЕ зависят —
   можно делать независимо.
3. Phase 5 (из анализа конкурента MeliSSA) — нужно сначала перечитать
   README.md, составить конкретный план по образцу фаз D1-D5.

**C. Продолжить чинить `pipelines`/чат** — отложено, но можно вернуться
(нужен `railway ssh`, ключ уже зарегистрирован).

**D. Решить судьбу Streamlit-легаси** — не срочно (пользователь явно не
поставил срок), но в какой-то момент стоит либо явно отключить сервис (экономия
на Railway), либо осознанно оставить навсегда как read-only архив.

## Промт для быстрого старта следующей сессии

Скопировать и вставить как первое сообщение:

```
Продолжаем call_center_dashboard. Прочитай NEXT_SESSION.md в корне репозитория.

Коротко о состоянии:
- Миграция на Dash (D1-D5) полностью завершена — dash-director теперь основной
  публичный дашборд: https://dash-director-production.up.railway.app
- Полный паритет со Streamlit + роли (executive/manager/employee), личный
  кабинет сотрудника с LLM-коучингом, вкладка «Звонки» с плеером/редакторами/
  тегами/комментариями
- Streamlit оставлен жить как страховка на откат (мягкий cutover, не отключали)
- 118 юнит-тестов, 1 skip
- Есть 320 неиспользованных синтетических звонков с реальным call_datetime за
  ~3 месяца — готовы для дневного/часового графика, если возьмёмся за это

Явного следующего пункта плана нет — миграция закрыта. Смотри NEXT_SESSION.md
→ «Что дальше» за вариантами (дневной/часовой график, Phase 4/5, починка
pipelines/чата, судьба Streamlit-легаси) и уточни у меня, что делаем.
```

Если контекст нужен — всё равно сначала прочитать README.md → «Статус деплоя»
и «Куда развиваться», чтобы не предлагать то, что уже сделано.
